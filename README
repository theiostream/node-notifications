Build instructions:

Preliminary:
- Install... node? (v8, etc. all that shit)
- Install node-gyp
- git submodule init or whatever

Build:
$ ./run_logos.sh
$ node-gyp configure
$ node-gyp build

Run:
$ node
> require('./')

Reference:
Ask Fudge.
